
<h1> All Bloggers </h1>
<% @bloggers.each do |blogger| %>
    <%= link_to blogger.name, blogger_path(blogger) %><br>
    <% end  %>
    <%= button_to "CREATE BLOGGER", new_blogger_path, method: :get %>
    <%= button_to "GO TO POST FEED", posts_path, method: :get %>